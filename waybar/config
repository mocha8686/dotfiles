{
	"layer": "top",
	"position": "top",
	"height": 30,
	"modules-left": [
		"disk",
		"cpu",
		"memory",
		"network"
	],
	"modules-center": [
		"hyprland/window"
	],
	"modules-right": [
		"tray",
		"pulseaudio",
		"clock#date",
		"clock#time"
	],
	"clock#date": {
		"interval": 60,
		"format": "  {:%a, %d %b %Y}"
	},
	"clock#time": {
		"interval": 1,
		"format": "  {:%T}"
	},
	"cpu": {
		"interval": 2,
		"format": "  {usage}%",
		"states": {
			"warning": 85,
			"critical": 95
		}
	},
	"disk": {
		"interval": 30,
		"format": "󰋊 {percentage_used}%",
		"states": {
			"warning": 85,
			"critical": 95
		},
		"tooltip-format": "{used} out of {total} used.\n{free} remaining."
	},
	"hyprland/window": {
		"separate-outputs": true
	},
	"memory": {
		"interval": 2,
		"format": "󰍛 {percentage}%",
		"states": {
			"warning": 85,
			"critical": 95
		},
		"tooltip-format": "{used}GiB out of {total}GiB RAM used. {avail}GiB remaining.\n{swapUsed}GiB out of {swapTotal}GiB SWAP used ({swapPercentage}%). {swapAvail}GiB remaining."
	},
	"network": {
		"interval": 10,
		"format-wifi": "󰖩  {essid} {signalStrength}%",
		"format-ethernet": "󰈁 {essid} {signalStrength}%",
		"format-disconnected": "󰖪 ",
		"tooltip-format-wifi": "{bandwidthDownBytes} download, {bandwidthUpBytes} upload.\nConnected as {ipaddr}/{cidr} via {ifname}.",
		"tooltip-format-ethernet": "{bandwidthDownBytes} download, {bandwidthUpBytes} upload.\nConnected as {ipaddr}/{cidr} via {ifname}."
	},
	"pulseaudio": {
		"format": "{icon} {volume}%",
		"format-icons": [
			"󰕿",
			"󰖀",
			"󰕾"
		],
		"format-muted": "󰝟 {volume}%"
	},
	"tray": {
		"icon-size": 16
	}
}
// vim:ft=json
